///
/// JDownloadTVSeriesResult.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "DownloadTVSeriesResult.hpp"

#include "EpisodeDownload.hpp"
#include "JEpisodeDownload.hpp"
#include <string>
#include <unordered_map>

namespace margelo::nitro::boxoffice {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "DownloadTVSeriesResult" and the Kotlin data class "DownloadTVSeriesResult".
   */
  struct JDownloadTVSeriesResult final: public jni::JavaClass<JDownloadTVSeriesResult> {
  public:
    static constexpr auto kJavaDescriptor = "Lcom/margelo/nitro/boxoffice/DownloadTVSeriesResult;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct DownloadTVSeriesResult by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    DownloadTVSeriesResult toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldEpisodes = clazz->getField<jni::JMap<jni::JString, JEpisodeDownload>>("episodes");
      jni::local_ref<jni::JMap<jni::JString, JEpisodeDownload>> episodes = this->getFieldValue(fieldEpisodes);
      static const auto fieldTotal = clazz->getField<double>("total");
      double total = this->getFieldValue(fieldTotal);
      return DownloadTVSeriesResult(
        [&]() {
          std::unordered_map<std::string, EpisodeDownload> __map;
          __map.reserve(episodes->size());
          for (const auto& __entry : *episodes) {
            __map.emplace(__entry.first->toStdString(), __entry.second->toCpp());
          }
          return __map;
        }(),
        total
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JDownloadTVSeriesResult::javaobject> fromCpp(const DownloadTVSeriesResult& value) {
      using JSignature = JDownloadTVSeriesResult(jni::alias_ref<jni::JMap<jni::JString, JEpisodeDownload>>, double);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        [&]() -> jni::local_ref<jni::JMap<jni::JString, JEpisodeDownload>> {
          auto __map = jni::JHashMap<jni::JString, JEpisodeDownload>::create(value.episodes.size());
          for (const auto& __entry : value.episodes) {
            __map->put(jni::make_jstring(__entry.first), JEpisodeDownload::fromCpp(__entry.second));
          }
          return __map;
        }(),
        value.total
      );
    }
  };

} // namespace margelo::nitro::boxoffice
